'use strict';

describe('myApp.twitter module', function() {

  beforeEach(module('myApp.twitter'));

  describe('twitter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var twitterCtrl = $controller('TwitterCtrl');
      expect(twitterCtrl).toBeDefined();
    }));

  });
});
